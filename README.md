# Mini-Shell
Mini Shell is a C-based system programming project that simulates basic functionality of a Unix/Linux shell. It supports execution of system commands, built-in commands, and process management using system calls such as fork, exec, and wait, demonstrating core operating system concepts.
